<?php
return array(
    /* 默认设定 */
	'APP_PATH'				=>	'./Application/',	//默认程序运行目录
    'DEFAULT_M_LAYER'       =>  'Model', 			// 默认的模型层名称
    'DEFAULT_C_LAYER'       =>  'Controller', 		// 默认的控制器层名称
    'DEFAULT_V_LAYER'       =>  'View',				// 默认的视图层名称
    'DEFAULT_W_LAYER'       =>  'Widget',				// 默认的Widget层名称
);

